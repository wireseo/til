# knowledge: bitwise and operator (100 & 011 = 000)
# concepts: X
# comment: should attempt using bitwise operators for smaller numbers and boolean return values
# runtime: X, not mine
# memory usage: X, not mine

class Solution:
    def canWinNim(self, n: int) -> bool:
        return n & 3